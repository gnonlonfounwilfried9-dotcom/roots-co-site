/* ROOTS - configuration de l'assistant.
   Laisser vide = l'assistant repond depuis la base de connaissances locale (118 fiches).
   Pour brancher le modele Claude, deployer le relais decrit dans worker/README.md
   puis coller ici son adresse, par exemple :
   window.ROOTS_CHAT_API = 'https://roots-chat.<votre-compte>.workers.dev';

   ATTENTION : ne jamais mettre de cle API dans ce fichier. Il est public.
   La cle reste dans le relais, cote serveur.
*/
window.ROOTS_CHAT_API = '';
